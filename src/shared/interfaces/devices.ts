export interface DevicesResponse {
    Device: {
        id: number,
        uuid: string,
        experimentId: string,
        createdAt: string,
        updatedAt: string,
        experimentValue: string
    }
}